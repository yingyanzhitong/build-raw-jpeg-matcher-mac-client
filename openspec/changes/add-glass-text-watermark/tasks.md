## 1. 本地字体与文字资产

- [x] 1.1 重新引入精简的 `fontdb`、`ab_glyph` 和 `imageproc` 文字特性依赖
- [x] 1.2 定义字体目录、文字请求、统一水印来源和尺寸基准的 camelCase 类型
- [x] 1.3 实现并缓存 macOS 本机字体枚举、稳定标识校验和默认字体回退
- [x] 1.4 实现单行文字校验、抗锯齿栅格化、透明玻璃材质生成与受限预览缓存

## 2. 统一预览与导出管线

- [x] 2.1 将图片路径导出请求迁移为图片/文字带标签来源，并统一准备玻璃资产
- [x] 2.2 让 Rust 尺寸、旋转、安全边距、平铺数量校验和导出支持宽度/高度尺寸基准
- [x] 2.3 注册字体目录与文字素材检查命令，并保持现有图片命令兼容

## 3. 前端交互与持久化

- [x] 3.1 扩展前端类型和 v4 设置，完成 v3/v2/v1 图片配置迁移与损坏恢复
- [x] 3.2 实现图片/文字切换、文字输入、本机字体选择、失效字体回退和延迟预览生成
- [x] 3.3 将文字模式尺寸控件显示为文字大小，并让 Canvas 按高度基准匹配 Rust
- [x] 3.4 将当前来源接入批量导出、就绪提示、日志、无障碍名称和错误状态

## 4. 测试与验收

- [x] 4.1 补充 Rust 字体枚举、文字校验、玻璃 alpha、尺寸基准、平铺和序列化测试
- [x] 4.2 补充前端 v4 迁移、来源隔离、字体回退和高度几何状态测试
- [x] 4.3 运行 `npm run test:state`、`npm run build`、`cargo fmt --check`、`cargo test` 和差异检查
- [x] 4.4 在原生 App 中验证中文文字、字体切换、三画幅文字大小、单个/平铺预览与导出一致性

## 5. 文字默认值与紧凑平铺

- [x] 5.1 将本地设置升级到 v5，分离图片与文字 profile，并迁移仍使用旧默认值的文字参数
- [x] 5.2 将文字默认通透度调整为 40%、默认平铺间距调整为短边 2%，保持图片水印默认值不变
- [x] 5.3 在 Canvas 与 Rust 中实现文字交错行平铺，并保持预览与导出一致
- [x] 5.4 补充 v5 迁移、来源配置隔离、文字默认值和交错几何测试
- [x] 5.5 运行前端状态测试、构建、Rust 格式检查与测试，并在原生 App 中验收

## 6. 图片与文字通透度感知一致性

- [x] 6.1 为文字玻璃素材增加有上限的 alpha 感知校准，并递增文字预览缓存版本
- [x] 6.2 补充文字校准增强填充与暗边、保持透明上限及复用统一通透度曲线的 Rust 测试
- [x] 6.3 运行前端状态测试、构建、Rust 格式检查与测试，并在原生 App 中对比相同通透度效果

## 7. 文字旋转网格平铺

- [x] 7.1 在前端按未旋转字宽/字高生成交错局部网格，并随文字角度旋转到 Canvas 坐标
- [x] 7.2 在 Rust 导出和容量校验中实现相同的文字旋转网格，保留图片平铺规则
- [x] 7.3 补充长文字旋转密度、偏移、边界覆盖、数量上限及预览导出坐标一致性测试
- [x] 7.4 运行前端状态测试、构建、Rust 格式检查与测试，并在原生 App 中验收紧凑平铺
