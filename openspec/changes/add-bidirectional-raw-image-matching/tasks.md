## 1. 方向中立模型与能力协议

- [x] 1.1 在 Rust 中定义 MatchDirection、MatchFile、方向中立 MatchResult、MatchResponse 和 ExportSummary，并保持 camelCase 序列化契约。
- [x] 1.2 在 TypeScript 中将 JpegInput、RawCandidate、selectedRaw 等方向专属类型迁移为 MatchFile、selectedCandidate 和 MatchDirection。
- [x] 1.3 在 Rust 中建立唯一的 JPG/JPEG/PNG 与十二种 RAW 格式定义，并实现 matcher_capabilities Tauri 命令。
- [x] 1.4 注册新的方向中立 Tauri 命令，同时暂时保留旧命令以支持前端分阶段迁移。
- [x] 1.5 添加 Rust/TypeScript 协议契约检查，验证方向枚举、结果字段和导出摘要字段一致。

## 2. 双方向输入收集

- [x] 2.1 将现有 JPEG 收集逻辑重构为按方向和格式策略工作的通用输入扫描器。
- [x] 2.2 为图片输入加入大小写不敏感的 PNG 支持，并覆盖文件选择、目录递归和混合输入跳过日志。
- [x] 2.3 实现 RAW 文件与目录输入，应用当前 RAW 格式筛选、最小文件大小和 macOS 元数据目录排除规则。
- [x] 2.4 保持按规范化物理路径去重，并保留不同路径但同主文件名的独立输入。
- [x] 2.5 为两个方向提供文本清单，并保留完整主文件名与末尾编号引用解析；RAW 清单带扩展名时遵守已启用格式。
- [x] 2.6 添加输入收集单元测试，覆盖 JPG/JPEG/PNG 大小写、十二种 RAW、嵌套目录、重复路径、无效文件和过小 RAW。

## 3. 双方向匹配引擎

- [x] 3.1 实现 match_counterpart_files，根据方向选择输入角色、候选扩展名和查找目录扫描策略。
- [x] 3.2 复用大小写敏感的物理文件主文件名精确匹配，并确保编辑后缀不会被自动剥离。
- [x] 3.3 保留两个方向手工引用的“先精确、再不区分大小写后缀”匹配行为。
- [x] 3.4 为零个、一个和多个候选生成 missing、matched 和 conflict 状态，并按规范化路径稳定排序候选。
- [x] 3.5 将跨目录或跨 JPG/JPEG/PNG 格式的同名图片候选统一标记为 conflict，不自动选择格式优先级。
- [x] 3.6 生成方向化扫描日志与匹配摘要，并确保重新匹配不会继承旧 confirmed 状态。
- [x] 3.7 添加双方向匹配单元测试，覆盖唯一候选、缺失、多个 RAW、多个图片、编辑后缀和手工引用。

## 4. 安全导出与逐项容错

- [x] 4.1 实现 export_matched_files，按方向导出 selectedCandidate，并通过查找目录与格式条件重新扫描可信候选集合。
- [x] 4.2 按规范化候选路径去重，确保多个输入指向同一候选时只处理和统计一次。
- [x] 4.3 保持单目录扁平导出，以原子不覆盖方式创建目标，并区分实际复制、已存在相同文件、符号链接和文件名内容冲突。
- [x] 4.4 对源文件缺失、不可读和单项复制失败进行逐项记录，继续处理其余候选。
- [x] 4.5 扩展导出摘要，包含 copiedCount、alreadyPresentCount、skippedMissingCount、skippedConflictCount、collisionCount 和 sourceErrorCount。
- [x] 4.6 添加导出单元测试，覆盖两个方向、唯一候选去重、不覆盖、相同内容、不同内容和单项失败不中断。

## 5. 前端双方向状态与 Tauri 接入

- [x] 5.1 定义 DirectionWorkspaceState，并初始化图片→RAW与 RAW→图片两套独立的会话状态。
- [x] 5.2 实现按方向更新状态的纯函数和 invalidateMatchState，统一清除当前方向结果、确认项与导出报告。
- [x] 5.3 实现方向切换、状态恢复和“只清空当前方向”，并在 collect、match、export 忙碌期间禁止切换。
- [x] 5.4 接入 matcher_capabilities，以后端格式列表驱动文件选择器与 RAW 格式控件。
- [x] 5.5 将文件、目录和全 WebView 拖放按当前方向路由到通用输入收集命令。
- [x] 5.6 接入新的匹配与导出命令，并将当前方向的输入、目录、格式、手工引用和结果作为参数。
- [x] 5.7 在 RAW→图片方向取消 RAW 格式时排除已有同格式 RAW 输入、记录日志并使结果失效。

## 6. 顶部方向切换器与输入流程

- [x] 6.1 建立 directionConfig，集中管理卡片标题、格式说明、目录名称、按钮、空状态、统计和日志角色文案。
- [x] 6.2 在侧栏标题下方实现紧凑方向切换器，显示当前方向并以带无障碍提示的轻量交换图标完成切换。
- [x] 6.3 保证上方始终渲染当前方向输入、下方始终渲染查找目录，切换时同步移动 RAW 格式控件。
- [x] 6.4 为两张卡片增加步骤编号、完成状态和下一步提示，同时允许用户在第一步完成前选择查找目录。
- [x] 6.5 将图片卡片文案统一为“图片”并明确支持 JPG/JPEG/PNG；两个输入模式均展示文本清单入口并使用方向化说明。
- [x] 6.6 根据当前方向和缺失条件控制查找可用性，选择或更换目录后只保存状态且不得自动开始匹配。
- [x] 6.7 验证方向连接器在窄窗口、侧栏滚动、键盘焦点和减少动态效果设置下仍可正常使用。
- [x] 6.8 实现 120ms 离场加 200ms 入场的上下交换动效；过渡期间锁定所有方向相关操作并在完成后恢复焦点与无障碍宣告。
- [x] 6.9 在输入或查找目录补齐两步条件后自动执行当前方向匹配，并保留“重新查找”作为显式复查入口。

## 7. 方向化结果、冲突复核与反馈

- [x] 7.1 将页面标题调整为“RAW / 图片配对”，并按当前方向更新头部统计、工具栏、空状态和运行日志。
- [x] 7.2 将结果表重构为方向中立列结构，并根据候选类型通过受控 Quick Look 缓存渲染 RAW 或图片预览。
- [x] 7.3 将冲突弹窗重构为“左侧输入、右侧候选”，根据方向更新标题、说明、预览、打开动作和确认按钮。
- [x] 7.4 对没有真实路径的手工文本引用隐藏打开与预览操作，并保留引用信息展示。
- [x] 7.5 实现唯一候选确认，将 conflict 更新为 confirmed，并确保匹配条件变化时确认失效、方向切换后可恢复。
- [x] 7.6 更新导出完成弹窗，区分 RAW/图片导出并展示全部新增摘要指标和错误数量。
- [x] 7.7 根据当前方向更新 Cmd+O、Cmd+Shift+O、Cmd+R、Cmd+E 的作用对象与快捷键提示。
- [x] 7.8 升级首次引导存储版本并生成方向化步骤，同时确保常驻步骤关系不依赖引导是否已读。
- [x] 7.9 合并结果标题与工作区操作栏，将实时统计移至工作区右下角以保留原生标题栏拖动，并为日志抽屉补齐工作区左侧留白。
- [x] 7.10 使用单个默认高度（24px）的自绘标题栏保留窗口控件；有更新时在其右侧显示蓝底白字“更新”，并为窗口操作与剩余拖动区启用所需权限。

## 8. 回归验证与清理

- [x] 8.1 添加前端状态测试，覆盖默认方向、忙碌禁切换、双方向状态恢复、清空当前方向和条件变化失效。
- [x] 8.2 运行 TypeScript 类型检查及前端测试，修复所有方向专属字段和硬编码 JPG/RAW 文案残留。
- [x] 8.3 运行 cargo fmt、cargo clippy 和 cargo test，确认双方向扫描、匹配、导出与序列化测试通过。
- [ ] 8.4 在 macOS Tauri 应用中手动验证文件选择、目录、拖放、文本清单、方向切换、冲突复核、打开原文件和双方向导出。
- [ ] 8.5 使用 JPG、JPEG、PNG、十二种 RAW、大小写扩展名、多个同名候选、失效路径和目标冲突完成手动测试矩阵。
- [x] 8.6 确认新前端完全使用方向中立协议后，移除旧 JpegInput/RawCandidate 类型、旧 Tauri 命令及重复格式常量。
- [x] 8.7 运行严格 OpenSpec 校验，并核对五项能力规格中的所有场景均有对应实现或自动化/手动验证记录。
- [x] 8.8 扩展前端状态测试，覆盖动效进行中不可重复切换，以及方向返回后两个工作区状态仍互不影响。
- [x] 8.9 添加 RAW 文本清单匹配测试，覆盖同名图片、末尾编号引用、格式筛选与方向状态隔离。
