## 1. 界面结构与状态隔离

- [x] 1.1 在 `src/App.tsx` 增加 `activeWorkspace` 状态，默认值为 `raw-jpeg-matcher`。
- [x] 1.2 在应用标题区下方新增 Tab 栏，提供 `RAW/JPEG matcher` 和 `添加水印` 两个入口。
- [x] 1.3 将现有 RAW/JPEG 匹配三栏界面抽取为独立工作区组件，并保持现有交互行为不变。
- [x] 1.4 新增水印工作区组件骨架，包含输入区、配置区、预览/列表区、统计和日志区。
- [x] 1.5 确认切换 Tab 时 RAW/JPEG 状态和水印状态互不清空、互不写入对方日志。

## 2. 水印类型与前端配置

- [x] 2.1 在 `src/types.ts` 新增水印输入、配置、导出摘要和后端响应类型。
- [x] 2.2 实现水印工作区的图片目录选择入口，并展示当前输入根目录路径。
- [x] 2.3 实现文字水印模式，支持输入水印文案并校验空文案不可导出。
- [x] 2.4 使用前端 Canvas 将文字水印渲染为透明 RGBA/PNG 水印位图，包含透明度和阴影效果。
- [x] 2.5 实现图片水印模式，支持选择 `.jpg`、`.jpeg`、`.png` 水印图片。
- [x] 2.6 实现水印参数控件：透明度、尺寸、自动去背景、背景容差、边缘柔化、阴影强度和布局模式。
- [x] 2.7 实现首张图片或示例画布预览，并确保配置变化后可刷新预览效果。

## 3. Rust 图片扫描与水印核心

- [x] 3.1 在 `src-tauri/Cargo.toml` 添加轻量图片处理依赖，优先使用 `image` 并限制到所需格式特性。
- [x] 3.2 新增图片输入扫描命令，递归发现 `.jpg`、`.jpeg`、`.png`，跳过不支持文件并记录日志。
- [x] 3.3 新增水印图片读取逻辑，将文字 Canvas 位图或图片水印文件统一转换为 RGBA 水印位图。
- [x] 3.4 实现图片水印自动背景转透明：边缘取色、颜色距离容差、alpha 处理和边缘柔化。
- [x] 3.5 实现水印尺寸缩放、整体透明度调整和透明阴影层生成。
- [x] 3.6 实现单点布局和平铺布局的水印叠加逻辑。
- [x] 3.7 保持现有 RAW/JPEG 匹配、RAW 缩略图和 RAW 导出 Rust 命令行为不变。

## 4. 水印导出工作流

- [x] 4.1 新增水印导出按钮，在缺少图片输入或缺少有效水印配置时禁用或提示。
- [x] 4.2 使用 Tauri 目录选择器选择水印导出目录，并启用 `canCreateDirectories: true`。
- [x] 4.3 新增 Rust 水印导出命令，批量读取输入图片、叠加水印并写入导出目录。
- [x] 4.4 导出时保留输入图片相对于输入根目录的子路径结构。
- [x] 4.5 导出时默认不得覆盖已有文件，目标已存在时记录文件名冲突并跳过。
- [x] 4.6 导出过程中单张图片失败时记录失败原因，继续处理其他图片。
- [x] 4.7 导出完成后展示成功、跳过、失败和文件名冲突数量摘要，并提供打开导出目录入口。

## 5. 验证与回归

- [x] 5.1 添加 Rust 单元测试，覆盖水印输入目录递归扫描和不支持文件跳过。
- [x] 5.2 添加 Rust 单元测试，覆盖纯色背景图片水印转透明和容差行为。
- [x] 5.3 添加 Rust 单元测试，覆盖水印导出不覆盖已有文件和摘要计数。
- [x] 5.4 添加 Rust 单元测试，覆盖导出保留相对目录结构。
- [x] 5.5 运行 `npm run build`，验证 TypeScript 与前端构建通过。
- [x] 5.6 运行 `cargo test`，验证 Rust 后端单元测试通过。
- [ ] 5.7 在 macOS 本地运行 Tauri 应用，手动验证 Tab 切换、文字水印、图片水印、自动去背景、新建导出目录和批量导出。
