## 1. 清理与骨架

- [x] 1.1 删除旧水印前后端、旧 OpenSpec 和旧水印截图
- [x] 1.2 精简 Rust 水印依赖并注册新的任务状态与命令集合

## 2. Rust 本地图像管线

- [x] 2.1 定义 camelCase 扫描、素材、画幅配置、导出事件与汇总类型及参数校验
- [x] 2.2 实现安全递归扫描、EXIF 方向宽高和稳定排序
- [x] 2.3 实现受限临时目录中的水印素材与照片预览缓存
- [x] 2.4 实现九宫格、短边尺寸、偏移、旋转钳制和透明 alpha 合成
- [x] 2.5 实现 JPEG 质量 100、PNG 无损编码及 EXIF/ICC 尽量保留
- [x] 2.6 实现来源重验证、相对目录、原子新建、同名跳过和逐项失败继续
- [x] 2.7 实现 IPC Channel 进度、单任务互斥和当前图片结束后取消
- [x] 2.8 添加扫描、几何、合成、元数据、安全导出与取消的 Rust 单元测试

## 3. React 图片水印工作区

- [x] 3.1 创建全新前端类型、纯状态函数、版本化持久化和状态测试
- [x] 3.2 实现来源目录、水印素材选择、恢复校验和日志状态
- [x] 3.3 实现方向校正样片、Canvas 实时水印预览和无样片占位
- [x] 3.4 实现三画幅切换、独立参数、九宫格、滑杆和同步操作
- [x] 3.5 实现上一张/下一张、窗口化缩略图、导出进度、取消和完成汇总
- [x] 3.6 将“图片水印”接入第三工作区、快捷键、循环标签导航和统一日志
- [x] 3.7 完成 980px 最小窗口适配、键盘焦点、VoiceOver 标签和非颜色状态提示

## 4. 验证

- [x] 4.1 运行并通过全部前端状态测试和生产构建
- [x] 4.2 运行并通过全部 Rust 测试
- [x] 4.3 启动 Tauri 应用检查工作区布局、预览、配置恢复和导出/取消主流程
- [x] 4.4 校验 OpenSpec 状态、残留引用和最终 Git 差异

## 5. 不透明素材与平铺增强

- [x] 5.1 扩展中文 OpenSpec，明确本地纯色背景透明化、平铺参数和安全边界
- [x] 5.2 扩展 camelCase 类型、v2 本地配置和前后端统一平铺几何
- [x] 5.3 实现 Rust 边缘连通背景透明化、透明预览、平铺合成和参数校验
- [x] 5.4 实现素材处理状态、单个/平铺控件、间距控制和 Canvas 实时预览
- [x] 5.5 补充透明化、平铺、v1 迁移和预览/导出一致性测试
- [x] 5.6 运行全量测试、生产构建并在原生应用中验收不透明 JPEG 与平铺导出

## 6. 透明玻璃水印修正

- [x] 6.1 修正规格，明确全部素材转为透明玻璃水印，并将透明度语义替换为通透度
- [x] 6.2 实现 Rust 主体蒙版、透明填充、外轮廓高光、内部颜色交界和通透度合成
- [x] 6.3 将公开配置迁移为 `clarity`、本地存储升级到 v3，并更新 Canvas 与“通透度”控件
- [x] 6.4 补充原生透明素材、纯色背景素材、多色标志、通透度和预览/导出一致性测试

## 7. 工作区视觉统一

- [x] 7.1 扩展中文 OpenSpec，明确水印工作区与匹配工作区的侧栏、工具栏、预览舞台和响应式层级
- [x] 7.2 将照片来源、水印素材和画幅配置重构为 312px 三步流程侧栏，并显示当前、待完成和已完成状态
- [x] 7.3 将顶部结果栏、空状态、真实预览和缩略图导航对齐匹配工作台，移除整面棋盘格与双重卡片边框
- [x] 7.4 补充流程状态测试，运行前端测试、生产构建和 OpenSpec 校验，并在原生 App 中对比验收

## 8. 可配置 JPEG 导出质量

- [x] 8.1 扩展中文 OpenSpec，明确全局 JPEG 质量范围、默认值、持久化、后端校验和 PNG 无损行为
- [x] 8.2 将本地配置升级到 v6，在水印侧栏增加默认 100 的 JPEG 导出质量控件并传入导出请求
- [x] 8.3 在 Rust 导出请求和编码管线中校验并应用 JPEG 质量，PNG 继续无损编码
- [x] 8.4 补充配置迁移、质量范围和编码差异测试，并运行全量构建与校验
