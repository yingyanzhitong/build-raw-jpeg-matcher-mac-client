# 仓库协作说明

## 提交与发布规则

- 用户只说“提交”时，仅创建本地 Git commit；不得创建或推送 tag、不得 `git push`、不得手动触发 GitHub Actions，也不得同步任何 Release。
- 只有用户明确使用“发布”一词时，才可执行发布流程：完成必要校验后，创建并推送正确的 tag，并按对应工作流触发 GitHub Actions 与发行同步。
- 用户只说“推送”而未明确“发布”时，先说明推送到会触发 Actions 的分支可能导致发布，并请求确认；不得自行创建 tag 或启动发布流程。
- 对 `main`、`licensed` 及其他会触发部署或发行工作流的分支，非发布场景禁止推送。
