## 1. 项目初始化

- [x] 1.1 创建一个新的 Tauri v2 macOS 应用项目，用于 RAW/JPEG 匹配工具。
- [x] 1.2 接入选定的前端栈，并启用 TypeScript 支持。
- [x] 1.3 添加目录选择和打开原始文件所需的 Tauri dialog 与 opener 能力。
- [x] 1.4 配置应用元信息、窗口尺寸和 macOS 开发设置。
- [x] 1.5 定义 JPEG 输入、RAW 候选、匹配结果、导出结果和日志条目的共享数据结构。

## 2. Rust 文件系统核心

- [x] 2.1 实现 `.jpg` 和 `.jpeg` 扩展名大小写不敏感检测。
- [x] 2.2 实现受支持 RAW 格式的扩展名大小写不敏感检测。
- [x] 2.3 实现从混合文件/目录输入中递归发现 JPEG 文件。
- [x] 2.4 实现 JPEG 路径去重和不支持输入的日志记录。
- [x] 2.5 实现从用户选择的 RAW 源目录递归发现 RAW 文件。
- [x] 2.6 实现精确主文件名匹配，并计算 `matched`、`missing`、`conflict` 状态。
- [x] 2.7 为扫描、去重、精确匹配、未找到结果和冲突结果添加 Rust 单元测试。

## 3. GUI 来源选择

- [x] 3.1 构建主界面上下分区布局，包含 JPEG 输入区、RAW 源目录选择器、工具栏、结果摘要和日志。
- [x] 3.2 接入 Tauri 拖放和手动选择入口，支持接收多个 JPEG 文件和目录。
- [x] 3.3 展示当前 JPEG 输入数量，以及已跳过/重复输入日志。
- [x] 3.4 接入 RAW 源目录选择器，并展示已选择路径。
- [x] 3.5 在缺少 JPEG 输入或 RAW 源目录时禁用或校验查找操作。

## 4. 匹配工作流

- [x] 4.1 添加前端操作，用于调用 Rust 匹配命令。
- [x] 4.2 渲染结果摘要，包含已匹配、未找到、冲突和已确认数量。
- [x] 4.3 渲染结果表格/列表，展示 JPEG 文件名、状态、选中 RAW 路径和候选数量。
- [x] 4.4 在日志面板中展示匹配进度和最终摘要。
- [x] 4.5 使用红色视觉状态标记冲突行。

## 5. 冲突复核

- [x] 5.1 点击冲突行时打开冲突复核弹窗。
- [x] 5.2 在弹窗左侧展示 JPEG 文件信息，并在可用时展示预览。
- [x] 5.3 在弹窗右侧展示所有 RAW 候选，包含文件名、路径、扩展名、大小和修改时间。
- [x] 5.4 添加双击处理，使用 macOS 默认应用打开 JPEG 或 RAW 候选原始文件。
- [x] 5.5 添加候选确认能力，并将结果状态从 `conflict` 更新为 `confirmed`。
- [x] 5.6 确保未解决冲突始终被排除在导出之外。

## 6. 导出工作流

- [x] 6.1 接入导出目标目录选择器，并展示已选择路径。
- [x] 6.2 实现 Rust 导出命令，将 `matched` 和 `confirmed` RAW 文件复制到选定目录。
- [x] 6.3 导出时跳过 `missing` 和未解决的 `conflict` 条目，并输出清晰日志。
- [x] 6.4 通过跳过目标文件名冲突来防止覆盖，并记录冲突日志。
- [x] 6.5 在日志面板中展示导出进度和最终复制/跳过/冲突数量。

## 7. 验证

- [x] 7.1 添加基于 fixture 的测试，覆盖嵌套 JPEG 目录和嵌套 RAW 目录。
- [x] 7.2 添加测试，验证带编辑后缀的 JPEG 文件名不会匹配原始 RAW 文件名。
- [x] 7.3 添加测试，验证导出会跳过未解决冲突和文件名冲突。
- [x] 7.4 运行前端类型检查和 Rust 测试。
- [ ] 7.5 在 macOS 本地运行 Tauri 应用，并手动验证拖拽、匹配、冲突复核、打开原始文件和导出。
